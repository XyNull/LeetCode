
public class Main {
	public ListNode reverseList(ListNode head) {
		ListNode tmp;
        while(head != null){
        	tmp = head;
        	head = head.next;
        	tmp.next = null;
        	head.next = tmp;
        }
        return head;
    }
}
